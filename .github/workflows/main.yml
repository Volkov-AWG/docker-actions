name: Autotests worker

on: push

jobs:
  docker:
    timeout-minutes: 20
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Start containers
        run: ./mongo/docker-compose up -d
      - name: drop containers
        run: ./mongo/docker-compose down

